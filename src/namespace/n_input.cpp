#include <namespace/n_input.h>

namespace input {
  std::string* mouse_pressed(){
      std::string* ret = nullptr;

      // if(glfwGetKey(GLFWwindow *window, int key))
      
      return ret;
  }  
};